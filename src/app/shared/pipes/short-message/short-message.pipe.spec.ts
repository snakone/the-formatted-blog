import { ShortMessagePipe } from './short-message.pipe';

describe('ShortMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
